# Model ID is used to select the correct model objcet
model_id: DDFN_C64B10_NL_Compressive
model_params:
  in_channels: 1
  k: 16
# Model name is used for saving results
model_name: ${.model_id}/k${.model_params.k}
