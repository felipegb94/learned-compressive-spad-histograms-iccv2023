# Plain Deep Boosting Model from Peng et al., 2020

# Model ID is used to select the correct model objcet
model_id: DDFN2D_Phasor2Depth1Freq
model_params:
  outchannel_MS: 8
  n_ddfn_blocks: 16

# Model name is used for saving results
model_name: ${.model_id}/B-${.model_params.n_ddfn_blocks}_MS-${.model_params.outchannel_MS}
data_loss_id: L1
