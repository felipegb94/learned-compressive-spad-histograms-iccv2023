
# Model ID is used to select the correct model object
model_id: DDFN2D_Depth2Depth2Hist_01Inputs
model_params:
  in_channels: 1
  outchannel_MS: 8
  n_ddfn_blocks: 24

# Model name is used for saving results
model_name: ${.model_id}/B-${.model_params.n_ddfn_blocks}_MS-${.model_params.outchannel_MS}
